/**
 * Forces email HTML to use light text colors for dark mode display.
 * Wraps in a container with CSS overrides that win against inline styles.
 */
export declare function forceEmailDarkMode(html: string): string;
